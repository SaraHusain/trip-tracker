import React from 'react';

const Settings: React.FC = () => {
    return (
        <div style={{ padding: '1rem' }}>
            <h1>TripTracker Settings</h1>
        </div>
    );
};

export default Settings;